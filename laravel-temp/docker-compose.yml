version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: cinegibi_mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: cinegibi
    ports:
      - "3306:3306"